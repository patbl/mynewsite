---
title: My Donations
---

%h3= current_page.data.title

%table.table.table-striped.table-bordered
  %caption This is a partial list of donations I've made since 2012. |
           Both contributions to and grants from my The Eleemosynary |
           Flying Orang−Utans Fund, my donor-advised fund, are |
           listed below, so there is some double-counting. |
  %thead
    %tr
      %th Date
      %th Organization
      %th Amount
      %th Notes
  %tbody
    - Donation.load_donations.each do |donation|
      %tr
        %td= donation.date
        - if donation.url
          %td= link_to donation.organization, donation.url
        - else
          %td= donation.organization
        %td= number_to_currency(donation.amount)
        %td= donation.note
